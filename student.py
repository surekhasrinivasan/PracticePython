# In python instead of struct we have class.
# Declare a function must be called def __init__(self, any arguments)

class Student:
    def __init__(self, name, dorm):
        self.name = name
        self.dorm = dorm